import React from 'react';
import { render } from '@testing-library/react';

import App from './App';

describe('Drag', () => {
  it('should render', () => {
    const {} = render(<App />);

    expect();
  });
});
