import React from 'react';
import { render } from '@testing-library/react';

import Drag from '.';

describe('Drag', () => {
  it('should render', () => {
    const {} = render(<Drag />);

    expect();
  });
});
